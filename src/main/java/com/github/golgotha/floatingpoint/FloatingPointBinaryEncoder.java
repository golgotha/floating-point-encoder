package com.github.golgotha.floatingpoint;

/**
 * Base interface for encoding floating-point number to binary format
 */
public interface FloatingPointBinaryEncoder {

    String encode(String decimal);

}
